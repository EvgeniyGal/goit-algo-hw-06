# 🏡 Домашнє Завдання 6: Графи та Алгоритми (networkX)

Цей репозиторій містить рішення для домашнього завдання, розділені на файли `task_1.py`, `task_2.py`, та `task_3.py`.

---

## ⚙️ Інструкція із Запуску Проєкту

Для коректної роботи проєкту необхідно створити віртуальне середовище та встановити бібліотеки `networkX` та `matplotlib`.

### 1. Налаштування Середовища та Залежностей

1.  **Створіть віртуальне середовище** (`.venv`) у кореневій теці проєкту:

    ```bash
    python3 -m venv .venv
    # Або: python -m venv .venv
    ```

2.  **Активуйте середовище:**

    | Операційна система       | Команда активації              |
    | :----------------------- | :----------------------------- |
    | **Linux/macOS**          | `source .venv/bin/activate`    |
    | **Windows (CMD)**        | `.\.venv\Scripts\activate.bat` |
    | **Windows (PowerShell)** | `.\.venv\Scripts\Activate.ps1` |

3.  **Встановіть необхідні бібліотеки:**
    ```bash
    pip install networkx matplotlib
    ```

## Завдання 1: Модель та Аналіз Графа

**Модельована мережа:** Транспортна мережа міста (7 ключових локацій).

| Характеристика              | Значення                          |
| :-------------------------- | :-------------------------------- |
| Кількість Вершин (N)        | 7                                 |
| Кількість Ребер (M)         | 9                                 |
| Вершини з найвищим ступенем | Центр, Житловий Масив (Ступінь 3) |
| Тип Графа                   | Ненаправлений                     |

---

## Завдання 2: Порівняння DFS та BFS

**Ціль пошуку:** Шлях від **Центр** до **Житловий Масив**.

### Порівняння шляхів

| Алгоритм                  | Принцип                                            | Шлях (Приклад)                          | Довжина (Кількість ребер) |
| :------------------------ | :------------------------------------------------- | :-------------------------------------- | :------------------------ |
| **BFS** (Пошук у Ширину)  | Знаходить **найкоротший шлях** за кількістю ребер. | `['Центр', 'Вокзал', 'Житловий Масив']` | 2                         |
| **DFS** (Пошук у Глибину) | Занурюється глибоко по одній гілці.                | `['Центр', 'Вокзал', 'Житловий Масив']` | 2                         |

### Обґрунтування різниці

1.  **BFS завжди знаходить найкоротший шлях за кількістю ребер.** У цьому графі мінімальна довжина до `Житловий Масив` становить 2 ребра. BFS гарантовано знаходить один із цих шляхів.

2.  **DFS знайшов найкоротший шлях випадково (залежить від порядку обходу).** DFS обирає першу доступну гілку і йде по ній до кінця. Хоча в цьому прикладі він знайшов найкоротший шлях, у загальному випадку він може знайти довший шлях, оскільки його мета — просто знайти будь-який шлях.

---

## Завдання 3: Алгоритм Дейкстри

У графі були додані ваги (відстань/час). Алгоритм Дейкстри знайшов найкоротші шляхи за **сумарною вагою**.

- **Наприклад, шлях від Центр до Житловий Масив:**
  - Найкоротший шлях: `['Центр', 'Вокзал', 'Житловий Масив']`
  - Мінімальна сумарна вага: **17**
